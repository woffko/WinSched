# WinSched 0.6.0 physical Process Monitor smoke

Date: 2026-08-28

Environment: physical Windows 11 Threadripper host with an existing WinSched
0.5.1 installation. The operator ran the accepted 0.6.0 graphical Setup from a
local NTFS Downloads path and performed the UI actions manually. No automated
mouse, keyboard, focus, screenshot, or window activation was used.

## Artifact and upgrade result

- Setup SHA-256:
  `5d4c25bc3bdf11100bf129aa2ad52b7553194ad5239a0974cce2b0d0646fef2a`
- Installed version: `winsched 0.6.0`
- Pre/post configuration SHA-256:
  `cbe33a966d8e0138d429210749a809511d7fa6135f09bf3d0efd05982c05518a`
- Service: Running, Automatic, LocalSystem, fixed Program Files path
- Status: schema 5, Scheduling enabled, Auto mode, Logging Off, no last error
- All five installed executable hashes matched the frozen accepted payload.
- Tray Startup, Process Monitor, and Settings shortcuts were present.

## Operator-confirmed UI smoke

The operator confirmed the instructed sequence completed successfully:

- a normal left click on the tray CPU icon opened Process Monitor;
- a second left click focused the existing single Monitor window;
- tray right click retained the service and Scheduling menu;
- the service/Scheduling header and required process columns were visible;
- an exact-rule action opened the Settings draft and was closed without Apply;
- process polling resumed after minimizing and restoring the Monitor window.

The Monitor was closed after the smoke. The subsequent read-only receipt found
one tray process and no residual Monitor process.

The authoritative privacy-minimized receipt is
`runtime/v0.6.0/physical-host-process-monitor-result.json`.

## Boundary

This smoke proves the exact installed artifact, upgrade preservation, service
state, shortcuts, and the operator-observed Process Monitor interactions on the
physical host. It is not a new performance comparison and does not alter the
previous `no_clear_effect` conclusion for broad Responsive CPU Sets.
